# vim: ft=zsh:ts=2:sw=2:noet
# shellcheck disable=all

_exists() {
	command -v "$1" >/dev/null 2>&1
}

_which_exists() {
	for cmd in "$@"
	do
		! ( _exists "$cmd" ) && continue
		echo "$cmd"
		return
	done
}

